const { EmbedBuilder, Collection, ActionRowBuilder, ButtonBuilder, StringSelectMenuBuilder, AttachmentBuilder } = require("discord.js");

module.exports = class Util {
    constructor(client) {
      this.client = client;
    };
    
    
}